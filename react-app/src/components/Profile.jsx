import React from "react";

export default function Profile() {
  const headingStyle = {
    display: "flex",
    flexDirection: "column",
    alignItems: "center",
  };
  return (
    <div style={headingStyle}>
      <h1>Profile</h1>
    </div>
  );
}
